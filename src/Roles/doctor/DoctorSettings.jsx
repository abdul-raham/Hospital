import React from 'react';
import { Typography, Box } from '@mui/material';

const Settings = () => {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography variant="body1">Here you can configure your settings.</Typography>
    </Box>
  );
};

export default Settings;
